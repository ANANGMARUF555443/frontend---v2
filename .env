# File ini untuk referensi kamu isi di Vercel (Project Settings > Environment Variables),
# BUKAN untuk di-upload ke GitHub.
# Pastikan .gitignore frontend sudah include ".env" sebelum push!

# URL backend Railway kamu (cek URL aktifnya di Railway > Settings > Domains,
# bisa saja sudah beda dari yang di bawah ini)
VITE_API_URL=

